# Copiar a keystore.properties (no se versiona) y rellenar. Sin este
# fichero, la variante release se firma con la clave debug (solo desarrollo).
storeFile=keystore/novaiptv.jks
storePassword=CAMBIAME
keyAlias=nova
keyPassword=CAMBIAME
